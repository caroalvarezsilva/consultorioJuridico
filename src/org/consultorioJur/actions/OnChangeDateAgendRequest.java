package org.consultorioJur.actions;

import org.openxava.actions.*;
import org.openxava.util.*;

public class OnChangeDateAgendRequest extends OnChangePropertyBaseAction {

	 public void execute() throws Exception{
//	        String value = (String)getNewValue();
//	        String condition = "";
//	        if ( Is.empty(value)) condition = "1=1";
////	        else condition = "upper(name) like '%" + value + "%'";
//	        else condition = "SELECT s.scheduleId, s.day, s.startTime, s.endTime, g.place, g.name FROM Schedule s INNER JOIN GROUPLAWCENTERSCHEDULE"
//	        		+ "			 gs ON s.scheduleId = gs.scheduleId INNER JOIN GroupLawCenter g ON g.groupId = gs.groupId WHERE s.day = 'Lunes'";
//	        
//	        getView().setDescriptionsListCondition("groupLawCenter", condition);    // we modify the condition of the combo 'state'
    }
	
}